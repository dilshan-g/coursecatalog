parameters:
  session.storage.options:
    cookie_domain: '.coursecatalog.docksal'
  twig.config:
    debug: true
  cors.config:
    enabled: true
    allowedHeaders: ['x-csrf-token','authorization','content-type','accept','origin','x-requested-with', 'access-control-allow-origin']
    allowedMethods: ['*']
    allowedOrigins: ['*']
